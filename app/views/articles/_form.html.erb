<%= form_for(article) do |f| %>
    <div>
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= f.label 'Titulo', class: "control-label col-md-1 col-md-offset-1" %>
      <div class="col-md-10">
        <%= f.text_field :title, class:'form-control' %>
      </div>
    </div>

    <div class="form-group">
        <%= f.label 'Bajada', class: "control-label col-md-1 col-md-offset-1" %>
        <div class="col-md-10">
          <%= f.text_area :lead, class:'form-control', rows:'3' %>
        </div>
    </div>

    <div class="form-group">
        <%= f.label 'Cuerpo', class: "control-label col-md-1 col-md-offset-1" %>
        <div class="col-md-10">
          <%= f.text_area :body, class:'form-control', rows:'10' %>
        </div>
    </div>

      <div class="form-group">
        <div class="actions">
          <div class="col-md-12">
          <%= f.submit 'Crear Noticia' %>
          </div>
        </div>
      </div>
</div>
<% end %>
